name: Update Base64 Subscription

on:
  schedule:
    - cron: '*/15 * * * *'  # هر ۱۵ دقیقه اجرا می‌شود
  workflow_dispatch:  # امکان اجرای دستی هم هست

jobs:
  update-base64:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Reset base64 folder
        run: |
          rm -rf base64
          mkdir -p base64
          echo "✅ پوشه base64 ریست شد."
          # ریست گزارش
          rm -f report.txt

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run main.py
        run: python main.py  # تغییر به main.py

      - name: Show report
        run: |
          cat report.txt
